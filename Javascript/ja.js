// Positions: 0,1,2,3,4,5,6
var exampleArray = [ 2, -5, 10, 5, 4, -10, 0];

process_data = function(array){
var b;
var results = [];
if b >= 8 {};
  for (a = 0; array.length; a++){a};
  for (b = 0; array.length; b++){b};
      for (a + b) === 0){results.push(a,b)};
      a++;
      b++;
  }

var results = process(exampleArray);
console.log(results);

//=> ["1,3","2,5","6,6"]
